export enum OrderStatus {
  initial,
  estimated,
  agreed,
  repaired,
  ended,
  canceled,
}

export enum MobileModel {
  iphone = 'iphone',
  samsumg = 'samsung',
  mac = 'mac',
  huwai = 'huwai',
}

export enum RepairType {
  line = 'line',
  liquid = 'liquid',
  speaker = 'speaker',
  camera = 'camera',
}
